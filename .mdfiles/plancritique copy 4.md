The file is unchanged in the three places I flagged. Let me be specific about what's still open and what's partially resolved.

**`flags` is still not enumerated, and it's now a more serious gap than I described.** Line 437 still reads `// node saves: ... P, flags`. Previously I flagged `unshard_` and `is_causal_` as hidden under that stub. But re-reading the forward comment on line 417 — `// (obtain local contiguous shards ... as in v1: narrow or pre_sharded)` — and the "DONE" item in the MHA out-of-scope section (`pre_sharded=true` accepted when `!load_balance_`) — GQA inherits pre_sharded acceptance from v1. That means `pre_sharded_` is also a live branch condition in the GQA backward: line 454 branches on it explicitly (`(pre_sharded_)` vs gather). So the node has three flags that need to be saved — `unshard_`, `is_causal_`, `pre_sharded_` — and none of them are named anywhere in the saved state, not even as examples under `flags`. `pre_sharded_` is the most dangerous omission: an implementer reading only the "node saves" list has no signal it exists, yet the backward takes a materially different code path depending on it (local return vs all-gather), and getting that wrong produces silently wrong gradient shapes rather than a crash. This is exactly the same category as the `unshard_` issue in MHA that took a full round to fix, and it needs the same treatment: list all three flags by name in the saved state comment.

**The `(pre_sharded_)` comment in the backward is no longer misleading — it's correct** now that pre_sharded is live for GQA. But that makes the saved-state gap above more critical, not less.

**The ws=4 item is partially resolved but still ambiguous.** Line 475 now says `ws=4 remains the mandated axis-coverage run (needs 4 GPUs)`. That sentence points back to the existing MHA ws=4 run, which does cover the `ulysses_combine`/`ulysses_partition` axis-transposition risk since those functions are shared. If the intent is "the MHA ws=4 run already satisfies axis coverage for GQA because the collectives are the same code", that's a defensible position — write it that way explicitly. If the intent is that ws=4 GQA configs also run, they need to be specified (e.g. `nq=16, nkv=4, P=4` for `nkv≥P` and `nq=16, nkv=1, P=4` for MQA). As written, a reader can't tell which of those is meant, and whichever is the actual intent should be stated plainly.

Fix the `flags` stub to name all three saved booleans, and clarify the ws=4 scope sentence, and this plan is clean.